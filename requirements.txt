# Core data stack
numpy>=1.26.0
pandas>=2.0.0
duckdb>=0.10.0
openpyxl>=3.1.0

# Environment & config
python-dotenv>=1.0.0

# LLM / API (pick ONE depending on what you use)
openai>=1.0.0
# azure-openai>=1.0.0   # uncomment only if using Azure OpenAI SDK







